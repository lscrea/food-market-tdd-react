describe('createHttpProductCatalogQuery', () => {
    // get la réponse http en json

    // créer une gateway
    // createHttpProductCatalogQuery('http://localhost:3000')

    // ton expect(gateway.execute()).resolves.toEqual(...)    ...